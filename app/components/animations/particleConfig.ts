// Particle animation configurations for different sections

export const contactParticleConfig = {
  background: 'transparent',
  minSize: 0.3,
  maxSize: 1.2,
  particleDensity: 6,
  particleColor: 'hsl(var(--primary))',
  speed: 0.15,
  connectionOpacity: 0.2,
  connectionWidth: 0.5,
  connectionDistance: 80,
  connectionColor: 'hsl(var(--primary) / 0.3)',
  interactive: true
};

export const sparklesConfig = {
  background: 'transparent',
  minSize: 0.3,
  maxSize: 0.8,
  particleDensity: 5,
  particleColor: 'hsl(var(--primary))',
  speed: 0.1
};

export const faqParticleConfig = {
  background: 'transparent',
  minSize: 0.2,
  maxSize: 0.5,
  particleDensity: 3,
  particleColor: 'hsl(var(--primary))',
  speed: 0.05
};

export const footerParticleConfig = {
  background: 'transparent',
  minSize: 0.2,
  maxSize: 0.6,
  particleDensity: 3,
  particleColor: 'hsl(var(--primary) / 0.4)',
  speed: 0.1,
  connectionOpacity: 0.1,
  connectionWidth: 0.3,
  connectionDistance: 60,
  connectionColor: 'hsl(var(--primary) / 0.1)',
  interactive: true
}; 